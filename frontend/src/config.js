const packageJson = require('./package.json');
const apiBaseUrl = packageJson.api.baseURL;

module.exports = {
  apiBaseUrl
};


const config = {

  API_SERVER: 'https://www.kalogeris-portal.net/api'

};